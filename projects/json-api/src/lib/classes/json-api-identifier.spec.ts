import { JsonApiIdentifier } from './json-api-identifier';

describe('JsonApiIdentifier', () => {
  it('should create an instance', () => {
    expect(new JsonApiIdentifier()).toBeTruthy();
  });
});
